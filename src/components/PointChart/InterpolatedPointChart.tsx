import { Renderer, TFramerSettings } from "../../framer";
import { PointChartProps } from "./PointChart";

type InterpolatedPointChartProps = PointChartProps & TFramerSettings

export default function InterpolatedPointChart({}: InterpolatedPointChartProps) {

}